[project]
name = "mediassist-openenv"
version = "0.1.0"
description = "MediAssist OpenEnv deployment config"
requires-python = ">=3.11,<3.13"
dependencies = [
  "Flask==3.1.2",
  "Pillow==11.3.0"
]

[project.scripts]
app = "app_web:app"
